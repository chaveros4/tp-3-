#include <iostream>
using namespace std;

int main() {
    int valor, suma = 0, maximo, minimo;
    float promedio;

    for (int i = 0; i < 10; i++) {
        cout << "Ingrese un valor: ";
        cin >> valor;

        suma += valor;
        if (i == 0) {
            maximo = minimo = valor;
        } else {
            if (valor > maximo) maximo = valor;
            if (valor < minimo) minimo = valor;
        }
    }
